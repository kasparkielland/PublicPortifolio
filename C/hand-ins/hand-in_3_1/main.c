#include <stdio.h>#include <limits.h>#define ARRAY_SIZE 10//Creating a function to find lowest value, uses float!float minimum(float a[], float n){    //Creates an integer min_value = INT_MAX, because !!!!EXPLAIN!!!!!    float min_value = INT_MAX;    //This loop with the if/else-sentence is checking if the number from the input array, a[], is less than min_value    //After it has checked, the variable, min_value, is set to the same value as the input array, a[].    for (int i =0; i < n; i++) {        if (a[i] < min_value) {            min_value = a[i];        }    }    // !!!!!WHY!!!!!    return min_value;}float maximum(float a[], float n){    //Creates an integer max_value = INT_MIN, because !!!!EXPLAIN!!!!!    float max_value = INT_MIN;    //This loop with the if/else-sentence is checking if the number from the input array, a[], is less than max_value    //After it has checked, the variable, min_value, is set to the same value as the input array, a[].    for (int i =0; i < n; i++)    {        if (a[i] > max_value)        {            max_value = a[i];        }    }    // !!!!!WHY!!!!!    return max_value;}//Creating the main functionint main() {    //Declares a constant to use as the size of the array    //Declares the array    float numbers[ARRAY_SIZE] = { 0 };    //Declares various variables    int count = 0;    float sum = 0;    float tempHolder = 0;    float median = 0;    //Creating a for-loop that runs until the variable 'i' is equal or larger than the ARRAY_SIZE    for (int i = 0; i < ARRAY_SIZE; i++){        printf("Enter a number: ");        //Pushes the entered number to the array        scanf("%f", &numbers[i]);        //Increases the count by one        count ++;        //Updates the sum by adding the current input-number to the sum        sum += numbers[i];    }    //Creates a for-loop whithin a for-loop to check to two numbers against each other    for (int i = 0; i < ARRAY_SIZE; i++) {        //sets the second for-loop variable ('j') to be 1 index higher        for (int j = i + 1; j < ARRAY_SIZE; j++) {            //IF a number in the array is larger than the number after it; run code. ELSE; continue loop            if (numbers[i] > numbers[j]) {                //Puts the first number in a temporarily variable                tempHolder = numbers[i];                //Says that the number AFTER the firstly checked number will "take it's place"                numbers[i] = numbers[j];                //The number AFTER the firstly checked number will now take the position the the FIRST checked number                numbers[j] = tempHolder;            }        }    }    //Calculates the median    median = (numbers[4] + numbers[5]) / 2;    //Prints out the result    printf("Minimum: %f\n", minimum(numbers, sizeof(numbers) / sizeof(float)));    printf("Maximum: %f\n", maximum(numbers, sizeof(numbers) / sizeof(float)));    printf("Sum: %f\n", sum);    printf("Average: %g\n", sum/count);    printf("Sorted: ");    //Creates a for-loop to print the, now sorted, numbers after each other    for (int i = 0; i < ARRAY_SIZE; ++i){        printf("%i ", (int)numbers[i]);    }    printf("\nMedian: %f\n", median);    return 0;}